# `core` Schema

**Tables in snapshot:** 0

This schema exists in the current structural export but contains no ordinary tables. It is reserved for future/shared domain use. Do not invent table semantics until structures are explicitly designed.